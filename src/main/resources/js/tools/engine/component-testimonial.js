flowY += COMPONENT_SPACING;
const card = penpot.createRectangle();
card.name = "Testimonial/Card";
card.x = columnX;
card.y = flowY;
card.resize(COMPONENT_WIDTH, 10);
card.borderRadius = 20;
card.fills = [{ fillColor: 'rgba(255,255,255,0.16)', fillOpacity: 1 }];
created.push(card);
const quote = penpot.createText("\u201cIncroyable qualité, livraison ultra rapide.\u201d");
quote.x = card.x + 24;
quote.y = card.y + 20;
quote.fontSize = 16;
quote.fills = [{ fillColor: TEXT_COLOR, fillOpacity: 0.95 }];
quote.resize(COMPONENT_WIDTH - 48, 10);
created.push(quote);
const author = penpot.createText("— Client vérifié");
author.x = card.x + 24;
author.y = quote.y + quote.height + 12;
author.fontSize = 14;
author.fontWeight = "bold";
author.fills = [{ fillColor: TEXT_COLOR, fillOpacity: 0.8 }];
author.resize(COMPONENT_WIDTH - 48, 10);
created.push(author);
const totalHeight = author.y + author.height - card.y + 20;
card.resize(COMPONENT_WIDTH, totalHeight);
flowY += totalHeight + 30;